{% extends 'base.html.twig' %}

{% block title %}Commande validée{% endblock %}

{% block body %}
<div class="container mt-5 pt-5 text-center">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6 fade-in">
            <div class="mb-4">
                <i class="bi bi-check2-circle display-1 text-dark"></i>
            </div>
            <h1 class="h3 mb-4 text-uppercase" style="letter-spacing: 2px;">MERCI</h1>
            <p class="text-muted mb-5">Votre commande a été enregistrée avec succès.<br>Un email de confirmation vous a été envoyé.</p>
            
            <a href="{{ path('app_product') }}" class="btn btn-primary px-5">CONTINUER</a>
            <div class="mt-4">
                <a href="{{ path('app_profile') }}" class="text-muted small text-decoration-underline text-uppercase">Voir ma commande</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
