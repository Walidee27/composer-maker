<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}E-Shop Vêtements{% endblock %}</title>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Playfair+Display:wght@400;700&display=swap" rel="stylesheet">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
        <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-light fixed-top">
            <div class="container">
                <a class="navbar-brand" href="{{ path('app_home') }}">OPULENTIA</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_home') }}">ACCUEIL</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_product', {'gender': 'WOMAN'}) }}">FEMME</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_product', {'gender': 'MAN'}) }}">HOMME</a>
                    </li>
                    </ul>
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_cart') }}">
                                <i class="bi bi-bag"></i> Panier
                            </a>
                        </li>
                        
                        {% if app.user %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_profile') }}">
                                    <i class="bi bi-person-circle"></i> Mon Compte
                                </a>
                            </li>
                            {% if is_granted('ROLE_ADMIN') %}
                                <li class="nav-item">
                                    <a class="nav-link text-warning" href="{{ path('app_admin') }}">Admin</a>
                                </li>
                            {% endif %}
                            <li class="nav-item">
                                <a class="nav-link text-danger" href="{{ path('app_logout') }}">Déconnexion</a>
                            </li>
                        {% else %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                            </li>
                            <li class="nav-item ms-2">
                                <a class="btn btn-secondary btn-sm rounded-pill px-3" href="{{ path('app_register') }}">Inscription</a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>

        <div style="margin-top: 80px;">
            {% block body %}{% endblock %}
        </div>

        <footer class="mt-5 border-top">
            <div class="container py-5">
                <!-- Newsletter Section -->
                <div class="row mb-5 justify-content-center text-center">
                    <div class="col-md-6">
                        <h5 class="text-uppercase mb-3" style="letter-spacing: 2px; font-size: 0.9rem;">Newsletter</h5>
                        <p class="text-muted small mb-4">Inscrivez-vous pour recevoir les dernières actualités de la Maison.</p>
                        <form class="d-flex justify-content-center">
                            <div class="input-group mb-3" style="max-width: 400px;">
                                <input type="email" class="form-control border-0 border-bottom rounded-0 px-0" placeholder="Votre adresse e-mail" style="background: transparent; box-shadow: none;">
                                <button class="btn btn-link text-dark text-uppercase p-0 ms-3 fw-bold" type="button" style="text-decoration: none; font-size: 0.8rem;">S'inscrire</button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- Links Section -->
                <div class="row gy-4">
                    <div class="col-md-3 col-6">
                        <h6 class="text-uppercase mb-4 fw-bold" style="font-size: 0.8rem; letter-spacing: 1px;">Service Client</h6>
                        <ul class="list-unstyled small">
                            <li class="mb-2"><a href="{{ path('app_contact') }}" class="text-decoration-none text-muted hover-link">Contact</a></li>
                            <li class="mb-2"><a href="#" class="text-decoration-none text-muted hover-link">Livraison & Retours</a></li>
                            <li class="mb-2"><a href="#" class="text-decoration-none text-muted hover-link">FAQ</a></li>
                        </ul>
                    </div>
                    <div class="col-md-3 col-6">
                        <h6 class="text-uppercase mb-4 fw-bold" style="font-size: 0.8rem; letter-spacing: 1px;">La Maison</h6>
                        <ul class="list-unstyled small">
                            <li class="mb-2"><a href="{{ path('app_qui_sommes_nous') }}" class="text-decoration-none text-muted hover-link">Qui sommes-nous ?</a></li>
                            <li class="mb-2"><a href="#" class="text-decoration-none text-muted hover-link">Carrières</a></li>
                            <li class="mb-2"><a href="#" class="text-decoration-none text-muted hover-link">Développement Durable</a></li>
                        </ul>
                    </div>
                    <div class="col-md-3 col-6">
                        <h6 class="text-uppercase mb-4 fw-bold" style="font-size: 0.8rem; letter-spacing: 1px;">Légal</h6>
                        <ul class="list-unstyled small">
                            <li class="mb-2"><a href="{{ path('app_mentions_legales') }}" class="text-decoration-none text-muted hover-link">Mentions Légales</a></li>
                            <li class="mb-2"><a href="{{ path('app_cgu') }}" class="text-decoration-none text-muted hover-link">CGU</a></li>
                            <li class="mb-2"><a href="{{ path('app_cgv') }}" class="text-decoration-none text-muted hover-link">CGV</a></li>
                            <li class="mb-2"><a href="{{ path('app_politique_confidentialite') }}" class="text-decoration-none text-muted hover-link">Confidentialité</a></li>
                        </ul>
                    </div>
                    <div class="col-md-3 col-6">
                        <h6 class="text-uppercase mb-4 fw-bold" style="font-size: 0.8rem; letter-spacing: 1px;">Suivez-nous</h6>
                        <div class="d-flex gap-3">
                            <a href="#" class="text-dark fs-5"><i class="bi bi-instagram"></i></a>
                            <a href="#" class="text-dark fs-5"><i class="bi bi-facebook"></i></a>
                            <a href="#" class="text-dark fs-5"><i class="bi bi-twitter-x"></i></a>
                            <a href="#" class="text-dark fs-5"><i class="bi bi-youtube"></i></a>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Bottom Bar -->
            <div class="border-top py-4">
                <div class="container text-center">
                    <p class="small text-muted mb-0" style="font-size: 0.75rem;">&copy; 2025 OPULENTIA - TOUS DROITS RÉSERVÉS</p>
                </div>
            </div>
        </footer>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </body>
</html>
