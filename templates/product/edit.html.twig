{% extends 'base.html.twig' %}

{% block title %}Modifier Produit - Admin{% endblock %}

{% block body %}
<div class="container mt-5 pt-5 fade-in" style="max-width: 800px;">
    <h1 class="display-6 mb-5 text-center">MODIFIER LE PRODUIT</h1>

    <div class="bg-white p-5 border mb-4">
        {{ include('product/_form.html.twig', {'button_label': 'METTRE À JOUR'}) }}
    </div>

    <div class="d-flex justify-content-between align-items-center">
        <a href="{{ path('app_product_index') }}" class="text-dark text-uppercase fw-bold text-decoration-none border-bottom border-dark pb-1" style="letter-spacing: 1px; font-size: 0.8rem;">Retour à la liste</a>
        {{ include('product/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
